class teste {

	method void chama () {
		
		(a,b,c,d);   // falta identificador
	
		return 0;
	}
}

main {
		
	obj.imprimir aluno[MAX][MAX2]);   // falta '('
	
	obj.imprimir(;		// falta ')'soma
	
	obj.(235.00, 10.7);  // falta identificador
	
	resultado = obj.soma(A,B);
}
