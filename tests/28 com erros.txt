const {
	int Max = ;   			// falta valor
	float taxa = 9.75;
	int  = 10; 			// falta identificador
}

class disciplina {
	variables {
		int codigo;
		string nome;
		int creditos;
		float nota;
	}
}

class pessoa {
	variables {
		string nome, endereco;
		int idade;
	}		
}

class aluno extends pessoa {
    variables {
		string curso;
		int matricula;
		float bolsa; 
		disciplina disciplinas[Max];   
	}


	method imprimir (aluno x[Max], int qtdade, int nro_disc) {   // falta identificador
		variables { int i, j;	}
	
		i = 0;
		while (i < qtdade) {
			j = 0;
			write(x[i].nome,x[i].endereco,x[i].idade,x[i].curso,x[i].matricula,x[i].bolsa);
		
			while(j < nro_disc) {
				write(x[i].disciplinas[j].codigo,x[i].disciplinas[j].nome,x[i].disciplinas[j].credito,x[i].disciplinas[j].nota);
				j++;
			}
		i++;	
		}	
		return 1;
	}

	method bool passou (aluno x, int idx) {
		{ float nota_final; bool flag;}  			// falta 'variables'
	
		if (x.disciplinas[idx].nota  7.0)  {  	// falta operador
			write("aprovado");
			flag = true;		
		}
		else {
			write("ficou de final...");
			flag = false;	
		}
		return ;  // falta expressao
	}
}

main {
	variables {aluno alunos[Max]; bool flag;
	
	imprimir(alunos[Max],Disciplinas);  // esperava '}'
	
	flag = passou(aluno[5],10);
	
	if (flag ==   { 				// falta operando e ')'
		write("parabens"); 
	}
	else { 
		write("vai estudar"); 
	}
}
